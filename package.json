{
  "name": "tomato-vault.github.io",
  "version": "1.0.0",
  "scripts": {
    "dev": "bundle exec jekyll serve --livereload",
    "build": "bundle exec jekyll build",
    "scan": "./scripts/scan-secrets.sh"
  }
}
