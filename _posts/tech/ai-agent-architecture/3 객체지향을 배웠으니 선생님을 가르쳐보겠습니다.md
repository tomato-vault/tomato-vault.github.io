---
layout: post
title: "[3편] 객체지향을 배웠으니 선생님을 가르쳐보겠습니다"
description: "오브젝트 디자인 스타일 가이드에서 가져온 객체 분류와 값 객체 설계"
date: 2026-09-11
categories: [tech]
tags: ["ai-agent-architecture", "AI", "Agent", "OOP", "TDD", "Workflow"]
permalink: /tech/3-teach-object-design/
series: "1인 개발자의 프로덕션 엔지니어링 운영체계"
series_key: "ai-agent-architecture"
series_order: 3
---

- table of contents
{:toc}

## Object Design Style Guide

내 TDD 워크플로우에서 프로그래밍 철학적으로 가장 중요한 것은 **Object-oriented programming(`OOP`)**라고 해도 무방하다. **『오브젝트 디자인 스타일 가이드』** (Object Design **Style** Guide, Matthias Noback)는 내게 `OOP`를 자세히 소개시켜준 책이다.

![오브젝트 디자인 스타일 가이드 표지](/assets/img/posts/ai-agent-architecture-2-book-cover.webp){:width="400" loading="lazy"}

Matthias Noback의 『오브젝트 디자인 스타일 가이드』 표지.
{:.figcaption}

이 책도 여러 번 읽었고 필사도 진행해서 보관 중이다.

![오브젝트 디자인 스타일 가이드 필사 디렉토리](/assets/img/posts/ai-agent-architecture-2-object-design-directory.webp){:width="400" loading="lazy"}

옵시디언 볼트에 전 챕터를 타이핑으로 필사해둔 『오브젝트 디자인 스타일 가이드』 디렉토리 구조.
{:.figcaption}

나는 개발과 관련된 공부를 하며 여러 번 마주친 단어 중, '우아하다'라는 단어가 너무 좋더라 *~~(듣고있나 우아한 형제들)~~*. 잘 아는 단어이지만 평상시에는 사용하지 않는 생소함 때문에 더 꽂히는 것 같다. 아무튼, `OOP` 개발 방법론은 나에게 참 '우아해' 보였다.
{:.note title="여담"}

지난 [[2편] 토큰 낭비라던 TDD가 어느새 제 종교가 되었습니다](/tech/2-tdd-religion/)에서 이야기했듯, 내 개발 프로세스는 `TDD`라는 일상 엔진과 검진 스킬이라는 안전망으로 지탱된다. 내 TDD 워크플로우는 크게 계획을 세우는 `tdd-plan`과 실행을 전담하는 `tdd-go`의 2단계로 나뉘는데, TDD 엔진이 제대로 돌아가려면 무엇보다 첫 단추인 계획 단계(`tdd-plan`)에서부터 코드가 지켜야 할 객체지향적 뼈대를 단단히 세워두어야 한다.

아무런 설계 가이드 없이 AI에게 "테스트 먼저 짜고 구현해"라고만 지시하면 어떻게 될까? AI는 절차지향적인 거대 함수를 찍어내거나, 온갖 원시 타입(`primitive type`)만을 덕지덕지 이어 붙인 취약한 코드를 만들어내기 십상이다. 테스트는 통과할지 몰라도 객체지향이 주는 유연성과 견고함은 온데간데없이 사라지는 것이다.

이번 3편에서는 『오브젝트 디자인 스타일 가이드』에서 영감을 얻어 내 TDD 계획 단계(`tdd-plan`)에 녹여낸 **5단계 객체지향 설계 원칙**을 집중적으로 소개해 보겠다.

### 1단계: 객체 분류: 서비스, 개체, 값 객체, DTO

첫 단계는 객체를 분류하는 작업이다. 요구사항에 대하여 다음 표의 객체 중 어떤 내용에 대한 것인지를 판별한다. 이렇게 판별된 객체가 신규와 수정에 대한 규칙, 그리고 테스트와의 연계 등을 전부 결정한다.

| Type | 역할 | 동등성 (Equality) | 가변성 (Mutability) | 자기 검증 |
|:---|:---|:---:|:---:|:---:|
| **Service** | 로직 수행 및 흐름 제어 | - | Immutable | - |
| **Entity** | 상태와 생명주기를 지닌 모델 | Identity (ID) | Mutable | O |
| **Value Object** | 고유한 불변 속성값 표현 | Value (값 동등성) | Immutable | O |
| **DTO** | 순수 데이터 전송 객체 | - | Mutable | X |

쉽게 예를 들자면, **값 객체(Value Object)의 예시로는 '만 원짜리 지폐'**가 있다. 일련번호가 달라도 지폐가 지닌 액면가(값)가 같으면 똑같이 만 원으로 취급하기 때문이다. 반면 **개체(Entity)의 예시로는 '학생'**을 들 수 있다. 동명이인이라 이름이 같더라도 각자의 고유한 정체성(학번이나 주민번호)을 지닌 엄연히 서로 다른 사람이기 때문이다.

### 2단계: 값 객체 탐색과 원시값 집착 해소

두 번째로는 값 객체 후보를 탐색한다. 값 객체란 불변이며 값 동등성을 가지고 있고, 생성 시점에 스스로 검증되는 자기유효성의 특성을 가지고 있다. 다음은 몇 가지 예이다.

```text
country_code + phone + is_verified      → Phone
present + late + early_leave + absent   → AttendanceCount
score + max_score + percentage          → Score
```

계획 단계에서는 이 후보를 항상 함께 다니는 원시값 묶음에서 찾는다. 코드베이스를 `grep`해서 같은 필드 두세 개가 여러 파일에 반복 등장하면 값 객체 후보로 본다. 값 객체가 필요한 이유는 원시값이 의미를 담지 못하기 때문이다.

```python
def send_verification(country_code: str, phone: str, is_verified: bool): ...
```

위 예시 함수를 보면 세 개의 값이 함께 다녀야만 의미가 있는데, 타입은 그걸 반영할 수가 없다. 그래서 다음과 같은 일이 생길 수 있다.

- `country_code`와 `phone`은 둘 다 `str`이라 순서를 바꿔 넣어도 통과됨
- 검증 로직이 부르는 쪽마다 복사됨
- 셋 중 하나만 빠뜨린 채 넘어가도 아무도 막지 않음

이것을 <mark class="kw-term">원시값 집착(Primitive Obsession)</mark>이라 하고, <mark class="kw-term">값 객체(Value Object)</mark>는 이를 해결한다. 값 객체는 특히 중요한데, 시스템 상에서 값 객체로 묶느냐 마느냐는 <mark class="kw-intent">의도</mark>가 들어가는 작업이며 애플리케이션 상에서 해당 객체의 행동을 결정짓기 때문이다. 다음은 그 "행동을 결정짓는다"는 것의 예시다.

```python
class Score:
    def __init__(self, score: int, max_score: int):
        if max_score <= 0:
            raise ValueError("만점은 0보다 커야 한다")
        if not 0 <= score <= max_score:
            raise ValueError("점수가 범위를 벗어났다")
        self._score, self._max = score, max_score

    def percentage(self) -> Decimal: ...     # 백분율 계산
    def is_pass(self, cut: int) -> bool: ... # 합격 판정
    def grade(self) -> str: ...              # 등급 산출
```

값 객체를 만들기 전에는 `percentage`, `is_pass`, `grade` 함수들이 서비스·라우터·프론트에 흩어져 있었을 것이다. 값 객체를 만드는 순간 이들을 묶을 곳이 생기는 셈이다. 만약 값 객체가 필요하여 새롭게 생성해야 한다고 판단이 되면 찾은 값 객체의 테스트를 **첫 단계 맨 앞에 배치**한다. 값 객체 테스트가 존재하면 이후 단계에서 그걸 쓸 수밖에 없기 때문이다*(설계 압력)*.

위 예시에서 보이다시피 값 객체는 생성 시점에 스스로 검증하는 특성을 가지고 있는데 이는 네 번째 단계와 이어진다.

### 3단계: CQS 원칙과 테스트 대역(Test Double) 분리

세 번째 단계는 객체간의 의존성을 셋으로 분류하여 어떤 종류의 테스트로 풀어낼지 계획한다.

```text
Query(값을 돌려받음)       → Stub / Fake.  반환값 → 결과로 검증
Command(부수효과를 일으킴) → Mock / Spy.   호출 여부·인자로 검증
System Boundary(외부 API)  → 인메모리 구현. 격리
```

이 항목은 <mark class="kw-term">Command Query Separation(CQS)</mark>의 개념에서 출발한다. CQS는 쉽게 이야기하면 이런 원칙이다. `Command`는 상태를 바꾸고 아무것도 반환하지 않는다. `Query`는 값만 반환하고 아무것도 바꾸지 않는다. 그리고 이 둘을 한 메서드에 섞지 않는다. 이유는 이 둘이 섞이는 순간 호출하는 쪽이 부작용을 예측할 수 없게 된다는 데에 있다. 아래는 간단한 예시이다.

```python
class Counter:
    def increment(self) -> None:      # Command: 변경하고 반환이 없음
        self._count += 1

    def current_count(self) -> int:   # Query: 반환하고 변경이 없음
        return self._count

    def increment_and_return(self) -> int:   # 섞여 있는 나쁜 예시
        self._count += 1
        return self._count
```

CQS를 적용하면 다음과 같은 사고를 미연에 방지할 수 있다.

- 로그 찍으려고 한 번 더 호출했더니 카운터가 두 번 올라감
- 디버거로 변수를 들여다봤더니 값이 변함
- "안전할 것 같아서" 순서를 바꿨더니 깨짐

`Query`는 몇 번 호출하든 상관없어야 하고, `Command`는 정확히 필요한 만큼만 호출돼야 한다. 이 둘이 섞이면 이 성질들이 무너진다. 이 개념만 해도 메서드, `call chain`, 객체 등의 레이어가 나뉘지만 여기서는 위에서 설명한 대로 메서드에 대해서만 다루도록 하겠다.

이 CQS 개념을 도입하게 되면 자연스럽게 <mark class="kw-term">테스트 대역(Test Double)</mark>이 나뉘게 된다. 이를테면 `Query`에 `Mock`을 쓰면 <mark class="kw-term">Mock 동어반복(tautology)</mark>이 된다.

```python
def test_converts_amount():
    mock_rates = Mock()
    mock_rates.exchange_rate_for.return_value = Decimal("0.8")   # Mock을 내가 설정하고
    ...
    assert result == Decimal("0.8")                              # 내가 꺼내서 확인
```

`Mock`은 호출 횟수·순서를 검증하는 도구인데, `Query`는 애초에 몇 번 부르든 상관없어야 하는 것이기 때문에 `Query`에는 `Stub`/`Fake`를 사용하여 반환값을 검증하고, `Command`는 `Mock`/`Spy`를 사용하여 호출 여부와 인자로 검증하도록 설계한다. 이러한 특성을 고려하여 `RED` 시점이 아니라 계획 시점에 설계를 해두는 식으로 작업하게 된다.

### 4단계: 생성자 테스트: 오직 실패 케이스만 검증한다

네 번째 단계는 생성자를 어떻게 테스트할지 정하는 것이다. 두 번째 단계에서 값 객체는 생성 시점에 스스로 검증한다고 했는데, 그러면 그 검증을 어떻게 확인할 것인가에 대한 이야기다.

처음에는 당연히 "잘 만들어지는지"부터 테스트하고 싶어진다. 그런데 내 계획서는 **실패 케이스만** 적도록 되어 있다. 이유는 두 가지다.

```python
def test_coordinates_can_be_constructed():
    coords = Coordinates(latitude=60.0, longitude=100.0)
    assert coords.latitude == 60.0     # 이걸 확인하려고
    assert coords.longitude == 100.0   # getter를 새로 뚫어야 한다
```

먼저 이 테스트는 아무 정보도 주지 않는다. 예외가 발생하지 않는 한 절대 실패하지 않기 때문에 회귀 신호가 0이다. 그리고 더 나쁜 것은, 저 `assert`를 쓰려고 아무도 안 쓰는 `getter`를 만들어야 한다는 점이다. 캡슐화를 지키자고 값 객체를 만들었는데 테스트가 그걸 도로 깨는 셈이다.

그럼 정상 생성은 누가 검증하는가. 행위 테스트가 이미 하고 있다. `Coordinates(0, 0).distance_to(Coordinates(3, 4))`가 5를 반환했다면 생성자도 제대로 작동했다고 볼 수 있다. 행위를 테스트하려면 객체가 먼저 제대로 만들어져야 하니까 말이다. 그래서 생성자는 거부해야 할 것을 거부하는지만 확인하면 된다.

```python
def test_latitude_must_be_within_range():
    with pytest.raises(ValueError, match="Latitude"):
        Coordinates(latitude=90.1, longitude=0.0)
```

`match=`가 붙어있는 이유는 이게 없으면 엉뚱한 이유로 발생한 `ValueError`도 통과해버리기 때문이다. 실제 실행 사이클에서는 "생성자 테스트면 `pytest.raises`만 썼는가", "예외 테스트면 `match=`를 넣었는가" 두 줄의 체크리스트로 강제된다.

이 항목이 재밌는 건 거꾸로 생각해봤을 때이다. 생성자 테스트에 실패 케이스만 있다는 것은, 결국 **잘못된 상태로 생성될 수 있는 경로를 전부 막았는가**를 묻는 것이 된다. '객체는 유효한 상태로만 생성되어야 한다'는 원칙이 테스트 목록의 형태로 나타난 셈이다.

### 5단계: 상태 변경 검증: getter 대신 도메인 이벤트를 보라

다섯 번째 단계는 상태 변경을 어떻게 검증할지 정하는 것이다. 만들 때 검증은 이미 했다. 그러면 만들어진 다음에 바뀔 때는 어떻게 할 수 있을까? 값 객체는 불변이라 애초에 안 바뀌지만 개체는 바뀐다.

여기서도 직관과 다르게 `getter`로 확인하지 않는다.

```python
def test_player_moves_left():
    player = Player(Position(10, 20))
    player.move_left(4)
    assert player.current_position == Position(6, 20)   # 내부 구조에 묶인다
```

네 번째 단계와 같은 문제다. 테스트를 위해 내부를 꺼내 보여줘야 하고, 그러면 나중에 위치를 저장하는 방식을 바꾸는 순간 동작은 그대로인데 테스트가 깨진다. 그렇다고 `move_left`가 새 위치를 반환하게 만들면 이번엔 세 번째 단계에서 이야기한 CQS를 어기게 된다. 게다가 반환값을 받았다고 해서 내부가 실제로 바뀌었다는 증명이 되지도 않는다.

그래서 개체가 기록한 <mark class="kw-term">도메인 이벤트(Domain Event)</mark>를 보게끔 한다.

```python
def test_player_moves_left():
    player = Player(Position(10, 20))
    player.move_left(4)
    assert PlayerMoved(Position(6, 20)) in player.release_events()
```

이벤트 목록 전체를 비교하지 않고 포함 여부만 확인하는 것이 포인트다. 그래야 나중에 이벤트가 하나 더 추가돼도 기존 테스트가 안 깨진다. 이벤트를 만들 만한 일이 아니면 `cart.is_empty()` 같은 `Query` 메서드로 확인해도 된다. 어느 쪽이든 공통점은 **객체가 스스로 공개하기로 한 것만 보고 판단한다**는 것이다.

## 객체 분류를 넘어 아키텍처로: The Passionate Programmer가 던진 화두

마티아스 노박의 객체 분류와 CQS, 값 객체 규칙으로 개별 객체들의 모양새는 잡혔다. 하지만 실제 프로덕션 코드를 작성하다 보면 더 거대한 질문과 마주하게 된다.

"이 객체들을 시스템 전체에서 어떻게 조립할 것인가?"
"요구사항이 계속 바뀌고 기능이 추가될 때, 기존 코드를 깨뜨리지 않고 어떻게 확장할 것인가?"

이 질문의 답을 찾던 중 마주친 유튜브 채널이 바로 40년 경력의 시니어 엔지니어 David Scott Bernstein의 **The Passionate Programmer** (*Beyond Legacy Code* 저자)였다. AI 시대를 맞아 그가 공개한 시리즈(AI-Driven Design Thinking 및 Discovering Design Patterns)는 내가 AI와 협업하며 겪었던 아키텍처적 갈증을 정확히 꿰뚫고 있었다.

### 1. 절차적 코드의 함정과 코드 경제학: 수정 비용은 작성의 5배다

AI(클로드나 챗GPT)에게 "이 기능 구현해줘"라고 요청하면, AI는 1초 만에 수십 줄의 코드를 거침없이 쏟아낸다. 언뜻 보면 잘 돌아가고 테스트도 통과한다. 하지만 그 코드를 자세히 뜯어보면 대부분 '절차적 코드(Procedural Code)'다. 조건문(`if/elif`)을 길게 늘어뜨려 각 단계(steps)를 순차적으로 나열한 형태다.

Bernstein은 세계 유수 기업들의 코드를 컨설팅하며 발견한 치명적인 착각을 꼬집는다.

> "절차적 코드를 단순히 `class` 문법으로 감싼다고 해서 객체지향이 되는 것이 아니다. 로직은 그대로인데 문법만 바꾼 것뿐이다. 절차적 코드는 '단계(steps)'를 설명하고, 진정한 객체지향은 '역할(roles)'과 상호작용을 모델링한다."

절차적 코드가 가진 가장 무서운 문제는 비즈니스가 바뀌는 순간 개발자와 싸우기 시작한다는 점이다. 새로운 결제 수단, 새로운 회원 등급, 새로운 할인 정책이 추가될 때마다 기존 함수 내부로 들어가 거대한 `if/elif` 블록을 수정해야 한다. 기존 코드를 수정한다는 것은 곧 기존에 잘 돌던 기능이 깨질 리스크를 떠안는다는 뜻이다.

여기서 Bernstein이 강조한 **코드 경제학(Code Economics)**이 뇌리를 때렸다.

> **"소프트웨어에서 코드를 수정하는 비용은 최초 작성 비용의 5배에 달한다."**
> 소프트웨어 전체 수명 주기 비용의 80%는 출시 이후의 유지보수에서 발생한다. AI는 이 수정 비용 문제를 해결해 주지 않는다. 오히려 구조적 모델 없이 코드를 찍어내면 기술 부채를 5배의 속도로 쌓아 올리며 문제를 노출시키고 증폭할 뿐이다.

AI 시대에 개발자의 역할은 '코드를 직접 타이핑하는 사람'에서 'AI에게 올바른 아키텍처 모델과 구조를 지시하는 사람'으로 완전히 바뀌어야 했다.

### 2. 패턴은 레시피가 아니라 힘(Forces)에 대한 반응이다

그렇다면 어떻게 코드를 조직해야 할까? 많은 개발자들이 디자인 패턴을 공부할 때 흔히 빠지는 함정이 있다. "할인 로직이니까 Strategy 패턴을 써야지", "객체를 만드니까 Factory 패턴을 써야지"처럼 패턴을 일종의 요리 레시피로 암기하고, 코드를 보며 적용할 곳을 찾아 헤매는 것이다.

하지만 Bernstein은 단호하게 말한다.

> **"패턴은 해결책이 아니다. 문제 공간에 작용하는 힘(Forces)에 대한 반응이자 신호다. 힘이 존재하지 않는데 패턴을 적용하는 것은 오버엔지니어링이자 연극(theater)에 불과하다."**

예를 들어 고객 유형이 일반과 VIP 단 두 개뿐이고 더 이상 변동 압력이 없다면, 단순한 `if`문 두 줄이 가장 우아하고 솔직한 코드다. 거기에 대고 `Strategy` 인터페이스를 만들고 클래스를 쪼개는 것은 낭비다.

하지만 "각 할인 정책이 독립적으로 진화하고 개별 교체되어야 한다"는 **독립적 변동(Independent Variation)**이라는 힘(Force)이 가해지기 시작할 때, 비로소 패턴이라는 해법이 자연스럽게 이끌려 나온다.

나는 이 통찰을 내 TDD 워크플로우의 첫 단추인 `tdd-plan`의 **Stage 1 (분석 단계)**에 핵심 원칙으로 녹여냈다.
- **Forces Analysis & CVA (Commonality & Variability Analysis)**:
  - "이 도메인에서 무엇이 변하고, 왜 변하는가?" (가변성, Variability)
  - "그 변동들이 공유하는 안정된 공통 구조는 무엇인가?" (공통성, Commonality)

변동만 보아서는 안 된다. 변동들이 공유하는 '공통 구조'를 명시적으로 질문해야만 인터페이스나 추상 클래스라는 추상화의 형태가 비로소 올바르게 도출된다.

### 3. 결정을 없애지 말고 옮겨라: Identity Coupling vs Interface Coupling

Bernstein의 강의 중 가장 강렬했던 깨달음은 결합도(Coupling)를 바라보는 시선이었다.

코드를 작성하다 보면 `if/elif` 분기문이 자꾸 늘어나서 찝찝해진다. 그런데 그 분기문이 나쁜 이유는 단순히 `if`가 많아서가 아니다.
한 함수 안에서 **Identity Coupling(타입을 검사하고 판별하는 결합)**과 **Interface Coupling(추상화를 통해 행위를 호출하는 결합)**이 한자리에 엉켜있기 때문이다.

```python
# Identity Coupling과 Interface Coupling이 뒤섞인 코드
class MeetingCoordinator:
    def schedule(self, meeting: Meeting) -> str:
        # 1. Identity Coupling: 타입을 검사하고 판별한다
        if meeting.kind == "team":
            return self._schedule_team(meeting)
        elif meeting.kind == "one_on_one":
            return self._schedule_one_on_one(meeting)
        # 2. Interface Coupling: 각 행위를 실행한다
        ...
```

새로운 미팅 종류가 추가될 때마다 `schedule` 메서드가 흔들린다. 변동의 원인이 한자리에 응집되어 있지 못하기 때문이다.

여기서 Bernstein은 명쾌한 해법을 제시한다. **"결정을 없애지 말고, 올바른 자리로 옮겨라 (Eliminate ≠ Relocate)."**

Strategy 패턴은 조건문이라는 결정을 없애는 마법이 아니다. 결정을 내리는 책임(Identity Coupling)을 **Factory**라는 단 한 곳으로 옮겨 격리하는 패턴이다.

- **Identity Coupling**: 오직 Factory 한곳에만 가둔다. 어떤 구체 클래스를 생성할지 결정하는 책임을 온전히 Factory가 짊어진다.
- **Interface Coupling**: 비즈니스 흐름을 담당하는 나머지 시스템 전체는 오직 추상화된 인터페이스만을 바라본다.

Factory를 시스템의 **안정성 경계(Stability Seam)**로 세우는 순간, 새로운 요구사항이 들어와도 기존 실행 코드는 단 한 줄도 건드리지 않고 Factory에 생성 규칙 하나만 추가하면 끝난다. 진정한 OCP(개방-폐쇄 원칙)가 달성되는 순간이다.

### 4. tdd-go의 실행 감시탑: Pattern Signal Detection

그렇다면 처음 기능을 설계할 때부터 온갖 패턴을 머릿속으로 예측하고 코드를 짜야 할까? 절대 아니다. 미래를 섣불리 예측하고 미리 패턴을 깔아두는 것은 전형적인 YAGNI(You Aren't Gonna Need It) 위반이다.

패턴은 출발하는 곳이 아니라, 리팩토링 과정에서 자연스럽게 '도착(Emergent Design)'하는 종착점이어야 한다.

그래서 나는 실제 TDD 사이클을 돌리는 실행 스킬인 `tdd-go`의 REFACTOR 단계(Step 6)에 **Pattern Signal Detection (패턴 신호 감지)** 규칙을 탑재했다. 코드를 작성하는 동안 시스템이 내뿜는 냄새와 신호를 관찰하다가, 임계점을 넘을 때 비로소 패턴을 적용하는 방식이다.

- **Identity Coupling의 성장**: 새로운 타입이나 비즈니스 룰이 추가될 때마다 같은 분기문이 계속 길어지는가?
- **거대한 테스트 (Large Test)**: 테스트 하나를 세팅하기 위해 Mock이나 협력자가 과도하게 많이 필요한가? *(테스트 작성의 고통은 객체의 응집도가 부족하다는 사실을 고발하는 가장 정직한 거울이다)*
- **Construction Leak**: 호출자가 구체 클래스(Concrete Class)의 생성 방식을 너무 많이 알고 있는가?
- **행위 정체성 상실**: 단순 메서드 호출이었던 작업에 실행 취소(undo), 큐잉(queue), 감사 로그(audit) 등의 요구가 생겼는가? *(Command 패턴 신호)*
- **Mode Smearing**: 같은 상태 분기(`if status == ...`)가 시스템 전반의 여러 핸들러로 번지고 있는가? *(State 패턴 신호)*
- **Polling과 시간 결합**: 한 객체가 다른 객체의 변화를 알아내기 위해 주기적으로 상태를 찔러보고 있는가? *(Observer 패턴 신호)*
- **조정 결합의 비대화**: 한 객체가 너무 많은 협력 객체들의 순서와 통신을 중재하며 신(God) 객체가 되어가고 있는가? *(Mediator 패턴 신호)*

이처럼 코드가 스스로 '나 지금 구조를 바꿔야 해'라고 신호를 보낼 때만 리팩토링 게이트가 열리고, 그 힘(Force)에 맞는 패턴을 정확한 자리에 안착시킨다.

### 5. AI와 힘(Forces)으로 대화할 때 열리는 차원

이것이 내가 David Scott Bernstein의 철학을 흡수하며 깨달은 **"AI 시대의 소프트웨어 아키텍처"**의 본질이다.

AI에게 단순 문법(Syntax) 수준으로 "이 함수 작성해줘", "이 조건문 추가해줘"라고 지시하면, AI는 영원히 절차적이고 깨지기 쉬운 레거시 코드를 양산한다. 그것은 AI의 한계가 아니라 개발자가 던진 지시의 한계다.

하지만 AI에게 **힘(Forces)과 경계(Seam)**의 언어로 대화하면 완전히 다른 세상이 열린다.

> "이 도메인에서 독립적으로 변하는 정책은 무엇이고, 변하지 않는 안정적인 뼈대는 어디인가?"
> "Identity Coupling을 Factory로 격리하고, 비즈니스 흐름은 오직 Interface Coupling으로만 연결하라."
> "새로운 타입이 추가될 때 기존 실행 코드가 수정되지 않도록 Seam을 세워라."

이러한 아키텍처 모델을 프롬프트와 컨텍스트로 제공하는 순간, AI는 사람 시니어 개발자조차 감탄할 만큼 우아하고 견고한 객체지향 설계를 놀라운 속도로 풀어낸다.

The Passionate Programmer 채널의 영상들에서 다루는 내용들은 주옥같아서 나중에 공부를 겸해서 이 블로그에서 시리즈로 다뤄볼까 생각 중이다.

## 5단계 객체 규약과 패턴 철학이 완성한 tdd-plan / tdd-go

정리하자면, 내 1인 개발 운영체계의 심장인 TDD 워크플로우는 두 거인의 어깨 위에 서 있다.

1. **Matthias Noback의 『오브젝트 디자인 스타일 가이드』**: 서비스, 개체, 값 객체, DTO의 4대 분류, 원시값 집착을 부수는 값 객체 설계, 부작용을 원천 차단하는 CQS, 실패 케이스 중심의 생성자 검증, 도메인 이벤트 기반의 상태 검증이라는 단단한 **'객체 단위 미시 규약'**을 제공했다.
2. **David Scott Bernstein의 『The Passionate Programmer』**: 절차적 코드의 수정 비용을 경계하는 코드 경제학, 힘(Forces)에 대한 반응으로서의 패턴, Identity Coupling과 Interface Coupling의 분리, 그리고 REFACTOR 시점의 12개 패턴 신호 감지라는 **'시스템 단위 거시 아키텍처'**를 완성시켜 주었다.

계획 단계(`tdd-plan`)에서 객체를 분류하고 힘(Forces)을 분석하여 안정된 Seam을 설계하고, 실행 단계(`tdd-go`)에서 RED-GREEN-REFACTOR를 돌리며 신호(Signal)에 따라 패턴에 안전하게 도착한다. AI가 아무리 방대한 양의 코드를 생성해도, 이 정밀한 아키텍처 그물망을 통과하는 순간 프로덕션 레벨의 단단한 자산으로 거듭난다.

하지만 아무리 객체 설계를 우아하게 다듬어도, 프론트엔드와 백엔드의 파라미터 계약이 어긋나거나 어디에서도 호출되지 않는 고립된 코드를 짜면 프로덕션은 한순간에 마비된다.

다음 글인 [[4편] 역시 내 테스트 스위트는 잘못됐다](/tech/4-all-passed-empty-screen/)에서는 실전에서 마주친 치명적인 경계 결함들을 막아낸 **경계 정합성 표 3종(계약·셰이프·트리거)**과, Mock 남발로 인한 **'All Passed' 착시 방지책**, 그리고 유출된 클로드 코드 아키텍처에서 착안한 **맥락 없는 독립 검토자(Coordinator)** 도입기를 다뤄보겠다.
